---
sidebar_position: 2
title: Examples
---

import CodePreview from '@site/src/components/CodePreview';
import VizMount from '@site/src/components/VizMount';
import { viz } from 'vizcraft';

export const basicBuilder = viz().view(800, 600);

export const basicScene = basicBuilder
  .node('node1')
  .at(400, 300)
  .circle(100)
  .label('Hello VizCraft', { dy: 5, fontWeight: 600 })
  .done();

Here you can see **VizCraft** in action.

## Basic Scene

This example demonstrates a simple scene with a single node.

<CodePreview code={`
import { viz } from 'vizcraft';

const builder = viz().view(800, 600);

builder
.node('node1')
.at(400, 300)
.circle(100)
.label('Hello VizCraft', { dy: 5, fontWeight: 600 })
.done();

const container = document.getElementById('viz-basic');
if (container) builder.mount(container);
`}>

  <div style={{ height: '400px', width: '100%', position: 'relative' }}>
    <VizMount builder={basicScene} style={{ height: '100%', width: '100%' }} />
  </div>
</CodePreview>
