---
'vizcraft': minor
---

Add resting and parked positioning options to the built-in `signal` overlay so signals can stay visible inside a node after arrival while still supporting routed edge motion.
