---
'vizcraft': minor
---

Add mount-level node and edge click subscriptions with `onNodeClick`/`onEdgeClick`, bubbling `vizcraft:*` DOM events, and default edge hit areas for reliable interactivity in embedded and `fromSpec` scenes.
